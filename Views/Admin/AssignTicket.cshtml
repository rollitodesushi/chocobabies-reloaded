@model dynamic
@{
    ViewBag.Title = "Asignar Ticket";
}
<h2>Asignar tiquete a participante #@ViewBag.RifaID</h2>
<p>Valor del numero: @ViewBag.PrecioTiquete</p>
<form asp-action="AssignTicket" method="post">
    <input type="hidden" name="rifaID" value="@ViewBag.RifaID" />
    <div class="form-group">
        <label>Nombre</label>
        <input name="nombre" class="form-control" required />
    </div>
    <div class="form-group">
        <label>Email</label>
        <input name="email" class="form-control" required />
    </div>
    <div class="form-group">
        <label>Telefono</label>
        <input name="numeroTelefonico" class="form-control" required />
    </div>
    <div class="form-group">
        <label>Numero</label>
        <input name="numeroTiquete" type="number" class="form-control" required />
    </div>
    <button type="submit" class="btn btn-primary">Asignar</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}